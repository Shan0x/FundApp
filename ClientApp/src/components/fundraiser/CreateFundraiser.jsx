/**
 * @name CreateFundraisers.jsx
 * @fileoverview A page containing a form for registered users to create
 * a new fundraiser.
 * @author Shannen Lowe
 * */
import React, { Component } from 'react';

export class CreateFundraisers extends Component {
  static displayName = CreateFundraisers.name;

  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div>
        <h1>Fundraiser Creation page</h1>
      </div>
    );
  }
}